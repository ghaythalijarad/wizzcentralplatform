version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static website - no build step required"
  artifacts:
    baseDirectory: .
    files:
      - '*.html'
      - '*.js'
      - '*.css'
