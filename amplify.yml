version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        build:
          commands:
            - echo "Static site deployment - no build required"
      artifacts:
        baseDirectory: .
        files:
          - '**/*'
        exclude:
          - node_modules/**/*
          - .amplify/**/*
          - amplify/backend/**/*
          - backend/**/*
          - .git/**/*
          - '*.log'
